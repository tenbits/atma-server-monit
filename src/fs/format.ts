export function format (arr: string[])
export function format (message: string)
export function format (mix: any) {

    if (typeof mix === 'string') {
        return mix;
    }
    if (Array.isArray(mix)) {
        
    }
}